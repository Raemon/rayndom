# Pinned to keep pip from picking pre-release pandas 3.0 / numpy 2.4, which
# triggers a cascade of resolver backtracking that ends up trying to build
# ancient matplotlib from source on Python 3.12 (where SafeConfigParser is
# gone). The upper bounds are intentionally generous within the known-good
# range, not exact pins.
py-feat==0.6.2
numpy>=1.26,<2.3
pandas>=2.0,<3.0
matplotlib>=3.8,<3.11
scikit-learn>=1.3,<1.9
scipy>=1.11,<2.0
torch>=2.0,<3.0
torchvision>=0.15,<1.0
opencv-python>=4.10,<5.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
