# Test dependencies for figure extraction module
pytest>=8.0.0
pytest-cov>=4.0.0

# Optional: for parallel test execution
# pytest-xdist>=3.0.0

# The main script dependencies (also needed for tests)
PyMuPDF>=1.23.0
Pillow>=10.0.0
requests>=2.28.0

# Optional: OpenCV for contour detection (speeds up figure splitting)
opencv-python>=4.8.0
numpy>=1.24.0
