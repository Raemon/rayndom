// Vertical space reserved for the fixed logging header (see LoggingLayoutClient). Pages that
// fill the viewport (e.g. zen) must subtract this from 100vh, and the content wrapper offsets
// itself by the same amount, so both reference this single value.
export const LOGGING_HEADER_OFFSET = '3rem'
